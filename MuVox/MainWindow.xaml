<Window xmlns:Recorder="clr-namespace:TTech.Muvox.Features.Recorder"
        xmlns:local="clr-namespace:TTech.Muvox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="TTech.Muvox.MainWindow"
        Style="{DynamicResource WindowStyle}"
        DataContext="{DynamicResource MainWindowModel}"
        Title="TTech - MuVOX"
        Width="684"
        Height="393"
        ResizeMode="NoResize" Icon="Resources/MuVox.ico" >
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Style.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <local:MainWindowModel x:Key="MainWindowModel" />
        </ResourceDictionary>
    </Window.Resources>
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Settings"
                      Command="{Binding ShowSettingsCommand}"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Grid>
        <ContentControl x:Name="CurrentView" Content="{Binding CurrentViewModel}"/>
    </Grid>
</Window>
